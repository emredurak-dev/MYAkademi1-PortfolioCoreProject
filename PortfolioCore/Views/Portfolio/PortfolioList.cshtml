@model List<Portfolio>
@{
    ViewData["Title"] = "PortfolioList";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}

<h1>projeler</h1>

@* <table class="table table-bordered">
    <tr>
        <th>#</th>
        <th>baslik</th>
        <th>aciklama</th>
        <th>kategori</th>
        <th>sil</th>
        <th>guncelle</th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.PortfolioId</td>
            <td>@item.Title</td>
            <td>@item.Description</td>
            <td>@item.Category.CategoryName</td>
            <td><a href="/Portfolio/DeletePortfolio/@item.PortfolioId" class="btn btn-danger">sil</a></td>
            <td><a href="/Portfolio/UpdatePortfolio/@item.PortfolioId" class="btn btn-info">guncelle</a></td>
        </tr>
    }
</table> *@

<div class="container-fluid">
    <div class="row">
        @foreach (var item in Model)
        {
            <div class="col-md-4 mb-4">
                <div class="card" style="width: 100%;">
                    <a href="@item.Link">
                        <img class="card-img-top" src="@item.ImageUrl" alt="Card image cap" style="width:100%; height:200px; object-fit: cover;">
                    </a>
                    <div class="card-body">
                        <h5 class="card-title">@item.Title / @item.Category.CategoryName</h5>
                    </div>
                    <ul class="list-group list-group-flush">
                        <li class="list-group-item">@item.Description</li>
                        <li class="list-group-item">
                            <a href="/Portfolio/DeletePortfolio/@item.PortfolioId" class="btn btn-outline-danger">sil</a>
                            <a href="/Portfolio/UpdatePortfolio/@item.PortfolioId" class="btn btn-outline-info">guncelle</a>
                        </li>
                    </ul>
                </div>
            </div>
        }
    </div>
</div>
<a href="/Portfolio/CreatePortfolio/" class="btn btn-outline-primary">yeni portfolyo ekle</a>